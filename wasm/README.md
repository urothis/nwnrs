We have not exported anything via macro yet.

Documentation: https://wasm-bindgen.github.io/wasm-bindgen/reference/types/exported-rust-types.html

```rust
#[wasm_bindgen]
pub fn add(a: u32, b: u32) -> u32 {
    a + b
}
```

Only build works so far.
