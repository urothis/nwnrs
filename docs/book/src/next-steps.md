# Next Steps

This book is now a scaffold for the workspace.

The next useful expansions are:

1. add per-crate "important types" tables with direct item links into docs.rs
2. add chapter pages for the largest format crates such as `gff`, `twoda`, `tlk`, `erf`, `mdl`, and `nwscript`
3. add workflow chapters that cut across crates
4. add architecture notes for fidelity boundaries, normalization steps, and lossiness

If the goal is a true reference book, the natural next pass is not more prose. It is denser API indexing.
